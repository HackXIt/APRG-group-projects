#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
#include "hidoku_solver.h"

using namespace std;

// Function to trace back and mark the solution path in the hidoku
// Given the end node, it traces back through the parent pointers to mark the path from start to end.
void printSolution(vector<string>& hidoku) {
    // TODO
    return;
}

// Main function to solve the hidoku using the CSP and alpha-beta-pruning algorithm
void solveHidoku(vector<string>& hidoku) {
    // TODO
    return;
}
